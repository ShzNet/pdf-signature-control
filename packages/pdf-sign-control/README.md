# pdf-sign-control

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build pdf-sign-control` to build the library.

## Running unit tests

Run `nx test pdf-sign-control` to execute the unit tests via [Vitest](https://vitest.dev/).
